apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: assettrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: 192.168.3.236/library/postgres:14
        env:
        - name: POSTGRES_PASSWORD
          value: postgres
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: assettrack
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
