apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namespace: assettrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rabbitmq
  template:
    metadata:
      labels:
        app: rabbitmq
    spec:
      containers:
      - name: rabbitmq
        image: 192.168.3.236/library/rabbitmq:3-management
        ports:
        - containerPort: 5672
        - containerPort: 15672
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: assettrack
spec:
  selector:
    app: rabbitmq
  ports:
  - port: 5672
  - port: 15672
